'use strict';

const chatroom = require('../lib/chatroom.js');

describe('chatroom module', () => {
  xit('should create a user with a nickname, id, and socket properties', () => {
    expect().toBeNull();
  });

  xit('should allow user to send message to all users on server', () => {
    expect().toBeNull();
  });

  //@quit
  xit('should allow user to quit out of server', () => {
    expect().toBeNull();
  });

  //@nickname <new name>
  xit('should allow user to chage nickname', () => {
    expect().toBeNull();
  });

  //list
  xit('should allow user to list all users on server', () => {
    expect().toBeNull();
  });

  //@dm <user> <message>
  xit('should allow user to directly message a specific user', () => {
    expect().toBeNull();
  });

  xit('server should return an error if an issue is found', () => {
    expect().toBeNull();
  });

  xit('server should remove client from client pool array', () => {
    expect().toBeNull();
  });

  //?
  xit('server should log data', () => {
    expect().toBeNull();
  });
});